#include <iostream>
#include <cmath>

using namespace std;

int main() {

    string lineOfNumbers;

    getline(cin, lineOfNumbers);

    printf("%s \n", lineOfNumbers.c_str());

    cout << lineOfNumbers << endl;

    return 0;
}